---
name: sdd-specify
description: 機能要求をEARS形式のrequirements.mdへ変換する。ユーザー可視の振る舞いを持つ新機能に着手する時、実装より先に使う。
---

このスキルの実体は `.agents/skills/sdd-specify/SKILL.md` にある(Agent Skills open
standardの実装場所を`.claude/skills/`(Claude Code)と`.agents/skills/`(Codex CLI等)の
両方に置き、単一の内容を重複させないための転送ファイル)。

起動したら必ず `.agents/skills/sdd-specify/SKILL.md` を読み、そこに書かれた
Output / When to use / Inputs / Steps / Stop condition / Boundary に従うこと。
